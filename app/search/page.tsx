import {demoVerses} from "../../lib/demo-corpus";
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const needle=q.toLowerCase();const hits=q?demoVerses.filter(v=>v.reference.toLowerCase().includes(needle)||v.osis.toLowerCase().includes(needle)||v.text.toLowerCase().includes(needle)||v.questions.some(x=>x.toLowerCase().includes(needle))):[];return <main><a href="/">← Симфония</a><p className="eyebrow">ПОИСК ПО ВСЕМУ КОРПУСУ</p><h1>{q?"«"+q+"»":"Введите вопрос"}</h1><form><input name="q" defaultValue={q}/><button>Найти</button></form>{hits.map(v=><section className="result" key={v.osis}><p className="eyebrow">ПИСАНИЕ</p><h2><a href={"/verse/"+v.osis}>{v.reference}</a></h2><p>{v.text}</p></section>)}<section className="result"><h2>Маршрут ответа</h2><p>Следующий слой выдачи: распознанные стихи → связанные вопросы → толкования → «Путь истины» → первоисточники. Каждый источник имеет provenance.</p></section></main>}