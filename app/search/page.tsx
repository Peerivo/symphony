export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;return <main><a href="/">← Симфония</a><p className="eyebrow">ПОИСК ПО ВСЕМУ КОРПУСУ</p><h1>{q ? "«"+q+"»" : "Введите вопрос"}</h1><form><input name="q" defaultValue={q}/><button>Найти</button></form><section className="result"><h2>Маршрут ответа</h2><p>В M1 этот экран станет единой выдачей: распознанные стихи → связанные вопросы → толкования → «Путь истины» → первоисточники. Каждый результат будет иметь provenance.</p></section></main>}